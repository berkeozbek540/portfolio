import PageTransition from "@/components/layout/PageTransition";

const page = () => {
  return (
    <PageTransition>
      <div className="py-4 px-8">Contact</div>
    </PageTransition>
  );
};

export default page;
